{
  "compilerOptions": {
    "outDir": "./dist/",
    //подсвечивает все места, где не указан тип
    "noImplicitAny": true,
    "module": "es6",
    "target": "es5",
    //в какую спецификацию компилируем: для поддержки большинства браузеров
    "jsx": "react",
    //компилятор будет обрабатывать не только TS файлы, но и JS файлы
    "allowJs": true,
    "moduleResolution": "node",
    //esModuleInterop позволяет работать с пакетами, которые
    //используют common js как с обычными пакетами с помощью import require() module.export
    "esModuleInterop": true,
    //если какая-то библиотека не имеет default import
    //лоадеры вроде ts-loader или babel-loader автоматтически создают их
    //вместо import * as React from 'react'
    //import React from 'react'
    "allowSyntheticDefaultImports": true
  },
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS"
    }
  }
}
